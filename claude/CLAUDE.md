## Implementation

- Clone throwaway/agentic git repos under `/tmp/mystuff/<repo>`, never bare `/tmp`.
  A `~/.gitconfig` includeIf on `/private/tmp/mystuff/` sets the personal author
  email; bare `/tmp` clones silently use the wrong work email.

@~/.claude-shared/prose-rules.md
